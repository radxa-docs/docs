import Network from "../radxa-os/_network.mdx";
import Locale from "../radxa-os/_locale.mdx";
import RemoteAccess from "../radxa-os/_remote-access.mdx";

## 用户名和密码

<table>
  <tr>
    <th>User name</th>
    <th>Password</th>
  </tr>
  <tr>
    <th>radxa</th>
    <th>radxa</th>
  </tr>
  <tr>
    <th>rock</th>
    <th>rock</th>
  </tr>
</table>

:::note
**注意：** 上述默认凭据（`radxa`/`radxa` 和 `rock`/`rock`）是正确的。如果您无法使用这些凭据登录，请尝试以下方法：

- 确保您已正确输入用户名和密码（注意大小写）
- 尝试使用 `root` 用户（无需密码，直接按 Enter）
- 尝试通过串口控制台登录进行故障排除

如果上述方法都无效，可能是因为系统在第一次启动时未完成用户创建过程（例如在创建用户过程中断电）。在这种情况下，请重新烧录系统镜像。
:::

## 更新系统

请参考 <a href={props.rsetup_path}>System Update</a>部分更新系统。

## 网络配置

<Network product={props.product} />

## 区域设置

<Locale />

## 远程访问

<RemoteAccess model={props.model} />
