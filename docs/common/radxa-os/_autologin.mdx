可以使用 `rsetup` 进行配置：

1. 终端运行 `rsetup`

2. 使用方向键和回车键依次进入 `User Settings` -> `Configure auto login`

3. 使用**空格**选择需要自动登录的会话类型，然后使用回车键确定

:::info
会话类型有以下几种：

gdm、sddm、lightdm: GUI 桌面管理器

serial-getty: 串口调试终端

getty: 屏幕终端
:::
