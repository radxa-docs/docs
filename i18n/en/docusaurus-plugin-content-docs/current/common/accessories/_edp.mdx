- Connect the EDP screen to {props.product} as shown in the figure.

  {" "}

  <img src={props.edp_img} style={{ width: "80%" }} />

- use <a href={props.rsetup_url} alt="rsetup url">rsetup </a> to enable {props.edp_overlay_name} Overlay

- After rebooting the system, the EDP screen is able to display the system screen.
